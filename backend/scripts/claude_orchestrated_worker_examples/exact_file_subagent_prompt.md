Use exactly one Agent subagent.

Have the subagent read only `backend/app/services/session_ingestion/adapters/claude_code.py` and return exactly one sentence stating what the file does.

Do not inspect any other files and do not add extra commentary.
