# Agent Hub Backend Configuration
# See root .env.example for the full variable reference.
# Copy root .env.example to .env.local (repo root) and fill in values.
#
# Key generation helpers:
#   AGENT_HUB_ENCRYPTION_KEY: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
#   AGENT_HUB_SECRET_KEY: python -c "import secrets; print(secrets.token_urlsafe(32))"
