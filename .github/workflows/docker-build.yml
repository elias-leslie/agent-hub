name: Build & Push Docker Images
on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      tag:
        description: 'Image tag override'
        required: false

permissions:
  contents: read
  packages: write

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - uses: pnpm/action-setup@v4
        with:
          version: 10.28.0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile
      - name: Backend checks
        working-directory: backend
        run: |
          uv sync --all-extras --dev
          uv run ruff check .
          uv run ty check app
          uv run pytest
      - name: Frontend checks
        run: |
          pnpm --filter frontend lint
          pnpm --filter frontend exec tsc --noEmit
          pnpm --filter frontend test

  pack-workspace:
    needs: check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.28.0
      - name: Pack workspace packages
        run: |
          mkdir -p /tmp/workspace-packages
          for pkg in chat-ui passport-client notes-ui; do
            if [ -d "packages/$pkg" ]; then
              cd "packages/$pkg"
              pnpm pack --pack-destination /tmp/workspace-packages
              cd -
            fi
          done
      - uses: actions/upload-artifact@v4
        with:
          name: workspace-packages
          path: /tmp/workspace-packages/*.tgz

  build:
    needs: pack-workspace
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - component: backend
            dockerfile: docker/backend.Dockerfile
            image-suffix: api
            needs-packages: false
          - component: frontend
            dockerfile: docker/frontend.Dockerfile
            image-suffix: web
            needs-packages: true
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/setup-qemu-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - if: matrix.needs-packages
        uses: actions/download-artifact@v4
        with:
          name: workspace-packages
          path: docker/workspace-packages
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ghcr.io/elias-leslie/agent-hub-${{ matrix.image-suffix }}:latest
            ghcr.io/elias-leslie/agent-hub-${{ matrix.image-suffix }}:${{ inputs.tag || github.ref_name }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
